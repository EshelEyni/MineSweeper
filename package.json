{
  "name": "mine-sweeper",
  "version": "1.0.0",
  "description": "A Clone for the Classic Game",
  "default": "index.html",
  "scripts": {
    "test": "vitest --reporter verbose --environment jsdom",
    "test:once": "vitest --run --environment jsdom",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/dom": "^9.3.1",
    "parcel": "^2.9.3",
    "process": "^0.11.10"
  },
  "dependencies": {
    "core-js": "^3.31.0",
    "regenerator-runtime": "^0.13.11",
    "vitest": "^0.32.2"
  }
}
